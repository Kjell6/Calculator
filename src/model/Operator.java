package model;

public enum Operator {
    PLUS,
    MINUS,
    MULTI,
    DIVIDE,
    POWER,
    SQRT,
    SIN,
    COS,
    TAN,
    FACULTY,
    LOGARITHM,
    MODULO,
    RECOPROCAL, //Kehrwert
    NONE;
}
